#!/usr/bin/env bash
set -euo pipefail

echo "[teardown] Module teardown is handled by shutdown commands now. Forwarding to tools/stop..."
exec "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/stop" "$@"
