#!/usr/bin/env bash
set -euo pipefail

echo "[teardown] DEPRECATED: module-level teardown is replaced by shutdown.sh. Forwarding to tools/stop..."
exec "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/stop" "$@"
