# Voice module configuration (env)
VOICE_ENGINE=piper

# Use a known-good Piper voice that exists in rhasspy/piper-voices
# and matches the file we keep locally in the repo.
VOICE_MODEL=en_US-john-medium

# Preferred directory for Piper models/configs. The launch script will also
# fall back to the repo's voices directory if this is not set.
PIPER_VOICES_DIR="$HOME/.local/piper/voices"

# Explicit URLs for the selected model/config so the node can download
# them if missing (no rebuild required).
PIPER_MODEL_URL="https://huggingface.co/rhasspy/piper-voices/resolve/main/en/en_US/john/medium/en_US-john-medium.onnx?download=true"
PIPER_CONFIG_URL="https://huggingface.co/rhasspy/piper-voices/resolve/main/en/en_US/john/medium/en_US-john-medium.onnx.json?download=true"

# Optional: legacy piper-tts library voice path (enables fallback path in node)
PIPER_VOICE="$PIPER_VOICES_DIR/en_US-john-medium.onnx"

VOICE_TOPIC=/voice
VOICE_INTERRUPT=/voice/interrupt
VOICE_RESUME=/voice/resume
VOICE_CLEAR=/voice/clear
