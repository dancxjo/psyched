cmake_minimum_required(VERSION 3.5)
project(wifi)

# Minimal CMakeLists to allow colcon/ament to scan this folder without
# failing when a package is intended to be an ament_python package.
find_package(ament_cmake REQUIRED)

ament_package()
