/**
 * Shared pilot utilities live here when we need to share code between the
 * overlay repo (`modules/pilot/pilot`) and the Fresh frontend. This placeholder
 * module keeps the directory tracked without exporting anything yet.
 */
export { };

