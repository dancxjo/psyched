[unit.faces]
apt = [
    "python3-opencv",
    "python3-numpy",
]
pip = [
    "opencv-python",
    "opencv-contrib-python",
    "numpy",
]
patches = []
launch = "modules/faces/launch_unit.sh"
shutdown = "modules/faces/shutdown_unit.sh"
pilot_control = "modules/faces/pilot/index.html"

[unit.faces.ros]
workspace = "."
packages = ["faces", "faces_msgs"]

[pilot]
display_name = "Faces"
description = "Detects faces, publishes crops, and emits embeddings"
regimes = ["perception"]
